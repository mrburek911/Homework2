{
  "name": "sweetshop-playwright-tests",
  "version": "1.0.0",
  "description": "Playwright tests for https://sweetshop.netlify.app (Homework assignment)",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "latest"
  }
}